flask>=3.0.0
flask-cors>=4.0.0
pypdf>=4.0.0
python-pptx>=0.6.23
numpy>=1.26.0
python-dotenv>=1.0.0
google-genai>=1.0.0
reportlab>=4.0.0
# Embedding-backed RAG (optional — the server falls back to a lightweight
# lexical vectorizer if this package cannot be installed in this environment):
# sentence-transformers>=2.7.0